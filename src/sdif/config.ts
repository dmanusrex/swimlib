/** Core SDIF configuration constants. */

/** Fixed length of each SDIF record. */
export const RECORD_LENGTH = 160;

/** Record separator in SDIF files. */
export const RECORD_SEP = '\r\n';

/** Default padding character for fields. */
export const FIELD_PADDING = ' ';
